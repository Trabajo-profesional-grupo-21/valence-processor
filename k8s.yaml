apiVersion: apps/v1
kind: Deployment
metadata:
  name: valence-processor
spec:
  replicas: 3
  selector:
    matchLabels:
      app: valence-processor
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: valence-processor
    spec:
      containers:
      - name: valence-processor
        image: <IMAGE>
        imagePullPolicy: IfNotPresent
        resources:
          limits:
            memory: "1000Mi" # Límite máximo de uso de memoria
          requests:
            cpu: "1000m" # Cantidad mínima de CPU requerida
            memory: "500Mi" # Cantidad mínima de memoria requerida
